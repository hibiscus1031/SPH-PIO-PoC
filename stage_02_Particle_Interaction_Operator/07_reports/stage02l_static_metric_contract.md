# Stage 02L — Static metric contract

Per-graph metrics are Q_L2, Q_Linf and cosine with `epsilon_metric=4e-10`. Equal-weight graph/family means, median, maximum, per-resolution and per-support summaries are frozen. Zero correction uses the theoretical Q_L2=1 baseline convention. No dataset performance was computed. Contract audit: **PASS**.
