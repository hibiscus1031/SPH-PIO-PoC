# Stage 02K — Feature contract

Feature audit: **PASS**. Inputs are restricted to baseline density, pressure, mass, smoothing length, relative velocity and legal reciprocal minimum-image graph quantities. Absolute position is geometry-only; absolute velocity is not a feature; `a_SPH` is audit-only. Target/reference/split/family/eligibility/regularity/ID/order inputs are absent.
