# Stage 01F2 MMS-A reference

MMS-A 粒子参考由闭式表达式直接生成：`x_unwrapped=x0+U_c t`、`y_unwrapped=y0`；仅在场评价和输出位置时 wrap 到 `[-1,1)`。未使用任何数值积分器。

已验证 `t=0, 0.01, 0.05, 0.1, 0.2`，并覆盖从 `x=0.99` 正向穿越周期边界的粒子。位置误差统一使用二维周期最小像位移，正向和负向跨界案例均通过。

结论：**PASS**。
