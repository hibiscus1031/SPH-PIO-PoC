# D3 Contract

`D3 = CAUSAL_TEMPORAL_RECIPROCAL_TRANSFORMER_PIO`.

D3 attends causally over the same legal accepted history `S_ref^(n-3:n)` and uses the same feature legality, graph, EOS, time-step, full RK2, and common reciprocal antisymmetric pair-head contracts. Future and target-derived tokens are prohibited; ephemeral RK2 midpoint state may not be committed as accepted history.

D3 is freshly initialized and receives exactly the common scale, loss, TRAIN origins, optimizer family, budget principle, checkpoint rule, and validation/test protocol. Stage 05A neither assumes nor claims that D3 is more trainable or performant than D1 or D2.
