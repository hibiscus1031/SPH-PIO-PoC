# D2 Contract

`D2 = CAUSAL_RECURRENT_PAIR_PIO`.

D2 may encode only the same legal accepted history `S_ref^(n-3:n)` through a causal recurrent pair representation. No future, midpoint-committed, target-derived, role-revealing, validation, or test token is legal. The learned output is consumed only by the common reciprocal antisymmetric pair head inside the unchanged full hybrid RK2 solver.

D2 receives fresh initialization and all fairness controls shared with D1/D3. Its recurrent state does not authorize cross-lineage memory or leakage between origins.
