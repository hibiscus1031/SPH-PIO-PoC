# D1 Contract

`D1 = INSTANTANEOUS_CONSERVATIVE_PAIR_MLP`.

D1 receives only the common preregistered instantaneous legal pair/node tokens at the current accepted state. It uses no temporal memory, future state, target-derived feature, lineage role label, or validation/test information. Its output passes through the common reciprocal antisymmetric pair head and the unchanged full hybrid RK2 transition.

D1 is a full comparator, not a deliberately weakened baseline. It receives fresh initialization and the same TRAIN origins, `s_a`, `L_def`, optimizer family, budget principle, checkpoint rule, and validation/test protocol as D2 and D3.
