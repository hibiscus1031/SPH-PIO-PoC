stage: "01C"
status: "PREREGISTERED_BEFORE_NATIVE_AUTOGRAD_RESULTS"
scope: "fixed-neighbor native-PyTorch value path; not a V2 solver"

layout:
  resolution: 16
  jitter_fraction: 0.05
  seed: 20261001
  domain: [[-1.0, -1.0], [1.0, 1.0]]
  support_ratio: 4.0
  dtype: "float64"

frozen_state:
  density: "1 + 0.05*sin(2*pi*x)"
  pressure: "0.05*sin(2*pi*x) - 0.03*cos(2*pi*y)"
  velocity_shape: "[sin(2*pi*x), cos(2*pi*y)]"
  mass: "dx^2"

rollout:
  positions_evolve: false
  density_evolves: false
  pressure_evolves: false
  neighbor_indices_evolve: false
  integrator: "explicit Euler value-path diagnostic"
  time_step: 0.0001
  steps: [1, 3, 5, 8, 16]
  loss: "mean(final_velocity^2)"

parameters:
  physical_viscosity: {base_value: 0.02, finite_difference_step: 0.000001}
  initial_velocity_amplitude: {base_value: 1.0, finite_difference_step: 0.000001}
  local_velocity_x_particle_0: {base_value: 0.0, finite_difference_step: 0.000001}
  local_pressure_particle_0: {base_value: 0.0, finite_difference_step: 0.000001}

qualification:
  steps_1_3_5_8:
    finite: true
    nonzero: true
    maximum_AD_FD_relative_difference: 0.01
  step_16:
    finite: true
    nonzero: true
    no_exception: true
    AD_FD_difference_is_diagnostic_only: true
  topology_differentiability_claimed: false
